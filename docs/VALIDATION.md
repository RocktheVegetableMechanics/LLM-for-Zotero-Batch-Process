# 3.9.6.16 同版本修订验证记录

验证日期：2026-09-21。Windows、Zotero 7、本机 Codex app server。此修订保持版本号 3.9.6.16 与插件 ID。

- 既有 12 个回归测试文件及新增队列持久化测试共 13 个文件通过；最后修改的空文件恢复与关闭监听器另经针对性复测。
- 覆盖回答保存失败后重试、并发保存合并、队列顺序和去重、提示词同步、运行时追加、重复启动、暂停/停止与落盘竞争、单篇失败继续、文献身份检查、会话保存回读及模型/推理配置。
- 队列 roundtrip 保留文献身份、提示词、配置和会话；运行项恢复为中断待确认；损坏与空文件保留并报错；存储错误阻止启动。故障注入使用模拟存储，没有破坏真实文库。
- 真实文库中使用 Luna / Max 和既有 My_Summary_Prompt 连续处理两篇论文，两篇均完成并确认回答保存。第二篇自动开始；运行期间切换论文、关闭独立对话窗口没有取消后台任务。
- 安装最终修订包后重启 Zotero，恢复两个完成项及一个等待项，队列保持暂停且未自动执行。等待项随后从测试队列移除，没有运行其分析。
- 重启后，从各篇论文会话和队列入口重新打开已完成回答，能够访问长正文、表格及结尾；测试会话保留。
- 最终修订包的短任务使用 Sol / Low，验证模型与推理选项实际传递、本篇完成后暂停、停止等待项、重新排队与继续。
- 短任务两篇均完成并保存；拖动、缩放、追加、去重、移除等待项和新建队列也通过实际界面操作验证。
- XPI 内 137 个文件与展开源码逐文件一致，实际安装包与待发布包一致。

单元和故障注入不能保证外部服务一直可用；真实模型输出的科学准确性、公式格式和外部解析器结果不属于本次软件回归的保证范围。原提交及原 XPI 已保留本地回退副本。
