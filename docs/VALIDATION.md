# 3.9.6.12 验证记录

归档检查日期：2026-09-15。

## 本次归档后执行

- 从本归档目录运行 Node.js 语法检查及 9 个回归测试，全部通过。
- 覆盖后台队列、批量请求配置、输入流恢复、委托状态显示、全文文档来源、MCP 等待配置、批量交互处理器、论文上下文归属及语义等待时间。
- 使用 scripts/build.ps1 成功重新生成 XPI。
- 将 dist 中归档 XPI 的全部 137 个文件逐字节与 src/extension 对比，全部一致。
- 测试没有调用真实模型或修改 Zotero 文库。个别旧测试打印历史实测字段，不代表本次又执行了一次网络测试。

## 安装版此前的真实验证

使用 GPT-5.6 Sol / Low 和原始长提示词连续完成两篇论文；确认逐篇执行、分别保存结果以及从其他文献切换回来后正确打开已保存会话。切换文库和追加任务没有取消当前请求；主动要求本篇结束后暂停时，当前篇完成后队列暂停。

最终版本没有重新实测 Luna / Max。文档来源报错的精确分支由读取实际 bundle 函数的回归测试验证；不声称模型实测覆盖所有 submit_document 路径。

未纳入含私人故障素材的旧 DeepSeek 和 skill_binding 测试。相关修复包含在发行包内，但不列入本次归档后 9 项通过结果。

队列未实现跨窗口关闭或重启恢复；已保存会话与待执行内存队列应区分。
